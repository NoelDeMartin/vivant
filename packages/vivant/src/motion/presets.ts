const presets = {
    fade: {
        initial: {
            opacity: 0,
        },
        enter: {
            opacity: 1,
        },
        leave: {
            opacity: 0,
        },
    },
};

export default presets;
